uid: TEST-0007
type: test
status: ratified
title: The gate loop runs every graph and exits non-zero when any fails
text: 'tests/test_multigraph.py, test_gate_loop: runs the loop read out of the skill
  text over a tree of three graphs where the consumer''s anchor item has been tombstoned;
  confirms a check group ran for each of the three graphs, every group ran tl, the
  exit status is non-zero, and the broken seam is reported. The repository''s own
  check workflow is the live instance of the same loop and runs this suite after
  it.'
rationale: '2026-09-13: the automated method now names the pytest that runs it rather
  than only the workflow.'
normative: true
links:
- target: REQ-0007
  type: verifies
attrs:
  method: automated
  origin: ai
  ratified_by: Henry Grech-Cini
  ratified_fingerprint: sha256:65b0e264a17aebe68e0c237d6cf7309883cb4dabd064c7972cf7a09971e81a03
  ratified_content:
    type: test
    text: 'tests/test_multigraph.py, test_gate_loop: runs the loop read out of the
      skill text over a tree of three graphs where the consumer''s anchor item has
      been tombstoned; confirms a check group ran for each of the three graphs, every
      group ran tl, the exit status is non-zero, and the broken seam is reported.
      The repository''s own check workflow is the live instance of the same loop
      and runs this suite after it.'
    normative: true
    derived: false
    attrs: {}
