# Assessment: tl-assess-skill

The tl:assess skill itself: a script, a skill text, a graph of fourteen items, seven fixture tests and a collection repository, built and then used on its own build, 13 September 2026.

Recorded 2026-09-13T15:30:37+00:00 from `/home/henry/Projects/01-applications/throughline-skills/skills/assess/idd`.

## Measured

| Measure | Value |
|---|---|
| Items in the graph (local, live) | 15 of 15 |
| Accepted (ratified, implemented or verified) | 15 (1.0) |
| Origin | ai 15 |
| Ratified by | Henry Grech-Cini 15 |
| Sources composed | none |
| Links into sources | 0 (0 stamped, 0 not) |
| Tests by method | automated 7 |
| Commits | 45 over 29.95 days, 0 tags |
| Commits naming an item | 39 (0.867) |
| Items amended after ratification | 1 of 15 |
| Lines added / removed | 6600 / 513 |
| Generated documents | 0 files, 0 words, current |
| Test files | 1, 13 pass, 0 fail |
| Sessions | 1 |
| Wall clock / active | 0.9 h / 0.6 h |
| Human turns / AI turns | 5 / 104 |
| Tool calls / API calls | 64 / 104 |
| Tokens out | 275,724 (thinking 66,343) |
| Tokens in (fresh / cache written / cache read) | 2,758 / 1,866,310 / 61,578,083 |
| Done | yes |
| Measured at commit | 4238b9149ea5 |
| Source pins | none |
| Tools | tl: tl 3.0.0; tl-compose: tl-compose 0.16.4 (throughline 3.0.0); tl-ratify: tl-ratify 0.7.0 (throughline-compose 0.16.4, throughline 3.0.0) |
| Skill | tl:assess 1.2.0 at 4238b9149ea5, Python 3.12.3 |

## What was built

The `tl:assess` skill in the throughline-skills plugin: a standard-library Python script with eight commands (graph, git, docs, tests, sessions, done, provenance, record), a skill text that fixes what the narrative may contain, a graph of fifteen items (one intent, seven requirements, seven tests) all ratified by one person, seven fixture tests (thirteen in the repository), catalogue entries at plugin 1.2.0, and a new private collection repository holding three cases: the register's specification, its implementation, and this one. The measured session is this one from 14:20 UTC, when the request arrived; the same session had reviewed the register that morning, which the `--since` window excludes. The git figures are for the whole plugin repository over its 30 days, not for this skill alone: the script does not yet scope git to a graph inside a shared repository, which this dogfooding found.

## Defects found after the first "done"

The first done was commit 9c26045: twelve tests green, plugin 1.2.0 pushed.

| Defect | Found by | Would a gate have caught it? | Which |
|---|---|---|---|
| No requirement to record the commit, the source pins and the versions of tl, tl-compose, tl-ratify and the plugin, so a number could not be compared with a later one | Henry, reading the result | No, a missing requirement | REQ-0007 and TEST-0007 added |
| A re-ratification counted as an amendment after ratification, overstating churn | The AI, reading the specification's first figures against what it knew of them | No | The measure now ignores changes to the ratification lines alone |
| Two records on one day overwrote each other, against the skill's own "never overwrite" rule | The AI, on writing the rule into the skill text | Yes, and it did once fixed to the minute | The fixture test failed on the minute-level fix |
| A commit was pushed with a failing test because a pipe masked pytest's exit code | The AI, reading the output after the push; CI on that commit failed | Yes, and it did | The repository's `check` workflow (run 34765532793) |
| Two records inside one second still collided: CI ran the fixture faster than the workstation | CI on commit 4238b91, the commit this record was measured at | Yes, and it did | The `check` workflow (run 34765737739); a suffix now guarantees the rule |
| `tl init` was run at `skills/assess` instead of `skills/assess/idd`, breaking the plugin's one-graph-per-skill layout | The AI, from a memory rule | No | Moved before the first commit |
| Four item titles carried a colon and the graph would not load | `tl check`, immediately | Yes, and it did | The parser |

Seven defects: one found by the human, two by CI, one by `tl check`, three by the AI reading its own output. This record's `tests_green` comes from the local run on commit 4238b91; CI on the same commit failed the record test by timing, and the fix is the following commit, 86e647e.

## What the graph caught before a person did

- `tl check` refused the graph on the first write: colons in four titles. Ten seconds, no ratification wasted.
- The strict gate listed the three items awaiting ratification and, after the naming rule changed, marked REQ-0006 ratified-stale, which is what sent it back to the human rather than letting the wording drift from the code.
- The repository's CI gate, which runs the fixture tests, caught both the masked failing test and the one-second collision; the local runs passed both times.

## Human effort

Five turns: the request with its purpose (objective cases for or against working this way, a done point each time), the remark that the skill could dogfood itself, the instruction to put it in throughline-skills, the preferred form of the ratification command, and the missing requirement for provenance. Two ratification sittings: fifteen items, then two new and one re-ratified.

## Verdict

Cost: 0.6 active hours of AI session, 276 thousand output tokens, 64 tool calls and 5 human turns for a script, a graph of fifteen items, seven tests, a catalogue entry, a collection and three recorded cases; one requirement had to come from the human after reading a result; two of the seven defects reached a pushed commit before CI caught them. Return: every number in the three cases came from a command anyone can rerun at the commit named, and the skill's own build is measured by the same rule as the work it measures. What it does not yet do: scope the git measure to one graph inside a shared repository, or count anything the transcript does not hold. Judgement: for a tool this small the graph's cost was minutes and its return was one wrong measure and two bad pushes caught by gates rather than by a reader; the case for it will be made or unmade by the cases it records, not by this one.
