# Assessment: tl-assess-skill

The tl:assess skill, recorded a third time after its git measure learned to follow an item through a rename: a script, a skill text, a graph of eighteen items, nine fixture tests and a collection of seven records, 13 September 2026.

Recorded 2026-09-13T18:37:23+00:00 from `/home/henry/Projects/01-applications/throughline-skills/skills/assess/idd`.

## Measured

| Measure | Value |
|---|---|
| Items in the graph (local, live) | 18 of 18 |
| Accepted (ratified, implemented or verified) | 18 (1.0) |
| Origin | ai 18 |
| Ratified by | Henry Grech-Cini 18 |
| Sources composed | none |
| Links into sources | 0 (0 stamped, 0 not) |
| Tests by method | automated 9 |
| Git scope | skills/assess, scripts/assess.py, tests/test_assess.py, docs/assessment |
| Commits | 15 over 0.15 days, 0 tags |
| Commits naming an item | 15 (1.0) |
| Items amended after ratification | 1 of 18 |
| Lines added / removed | 2572 / 84 |
| Generated documents | 0 files, 0 words, current |
| Test files | 1, 9 pass, 0 fail |
| Sessions | 1 |
| Wall clock / active | 1.0 h / 0.8 h |
| Human turns / AI turns | 7 / 128 |
| Tool calls / API calls | 76 / 128 |
| Tokens out | 314,798 (thinking 72,820) |
| Tokens in (fresh / cache written / cache read) | 3,376 / 1,909,863 / 77,744,418 |
| Done | yes |
| Measured at commit | 16dffbd2c037 |
| Source pins | none |
| Tools | tl: tl 3.0.0; tl-compose: tl-compose 0.16.4 (throughline 3.0.0); tl-ratify: tl-ratify 0.7.0 (throughline-compose 0.16.4, throughline 3.0.0) |
| Skill | tl:assess 1.3.0 at 16dffbd2c037, Python 3.12.3 |

## What was built

The `tl:assess` skill in the throughline-skills plugin: a standard-library Python script with eight commands (graph, git, docs, tests, sessions, done, provenance, record), a skill text that fixes what the narrative may contain, a graph of eighteen items (one intent, eight requirements, nine tests) all ratified by one person, nine fixture tests, and a private collection repository that now holds seven records: the register's specification, its implementation, the shape skill twice, and this skill three times. This third record follows the fix that taught the git measure to follow an item through a rename: the shape skill's first record had reported zero items amended after ratification where six is right, because the measure read each item at its current path and the skill's directory had moved. The measured session is the one from 14:20 UTC that built the skill, unchanged since the second record; the rename fix, its fixture test and this record were done in the shape skill's session of 13 September, which that work dominated, so it is excluded and its half hour is not in the table. The test count is this skill's own suite; the earlier records counted the repository's whole suite under the same heading.

## Defects found after the first "done"

The first done was commit 9c26045: twelve tests green, plugin 1.2.0 pushed.

| Defect | Found by | Would a gate have caught it? | Which |
|---|---|---|---|
| No requirement to record the commit, the source pins and the versions of tl, tl-compose, tl-ratify and the plugin, so a number could not be compared with a later one | Henry, reading the result | No, a missing requirement | REQ-0007 and TEST-0007 added |
| A re-ratification counted as an amendment after ratification, overstating churn | The AI, reading the specification's first figures against what it knew of them | No | The measure now ignores changes to the ratification lines alone |
| Two records on one day overwrote each other, against the skill's own "never overwrite" rule | The AI, on writing the rule into the skill text | Yes, and it did once fixed to the minute | The fixture test failed on the minute-level fix |
| A commit was pushed with a failing test because a pipe masked pytest's exit code | The AI, reading the output after the push; CI on that commit failed | Yes, and it did | The repository's `check` workflow (run 34765532793) |
| Two records inside one second still collided: CI ran the fixture faster than the workstation | CI on commit 4238b91 | Yes, and it did | The `check` workflow (run 34765737739); a suffix now guarantees the rule |
| `tl init` was run at `skills/assess` instead of `skills/assess/idd`, breaking the plugin's one-graph-per-skill layout | The AI, from a memory rule | No | Moved before the first commit |
| The git measure was repository-wide, so the first record of this case reported thirty days of four skills' commits | The AI, reading the first record | No, a missing requirement | REQ-0008 and TEST-0008 added; the second record is scoped |
| Four item titles carried a colon and the graph would not load | `tl check`, immediately | Yes, and it did | The parser |
| The git measure read each item's history at its current path only, so a graph whose directory had moved lost every ratification before the move and every amendment made in it; the shape skill's first record reported 0 of 20 amended where 6 is right | The AI, reading the shape skill's record | No, a missing case | The measure walks each file with rename following and compares consecutive states of the item, 80c5cf3; TEST-0009 |
| The first version of that fix stopped at the rename anyway: git's follow walk ends there when combined with reverse ordering | The new fixture test, failing before any commit | Yes, and it did | The walk runs forward and is reversed in Python, same commit |

Ten defects: one found by the human, two by CI, one by `tl check`, one by a fixture test, five by the AI reading its own output. Two of the ten reached a pushed commit before a gate caught them; the last two never left the working tree.

## What the graph caught before a person did

- `tl check` refused the graph on the first write: colons in four titles. Ten seconds, no ratification wasted.
- The strict gate listed the three items awaiting ratification and, after the naming rule changed, marked REQ-0006 ratified-stale, which is what sent it back to the human rather than letting the wording drift from the code.
- The repository's CI gate, which runs the fixture tests, caught both the masked failing test and the one-second collision; the local runs passed both times.
- The rename fix's own fixture test failed on the first version of the fix, which is how the reverse-ordering trap was found before a commit.
- After the fix, the strict gate on this graph reported TEST-0009 unratified until the human accepted it, and CI on main was red for the three minutes between the fix and the ratification commit, as it should be.

## Human effort

Seven turns in the measured session: the request with its purpose (objective cases for or against working this way, a done point each time), the remark that the skill could dogfood itself, the instruction to put it in throughline-skills, the preferred form of the ratification command, the missing requirement for provenance, the instruction to add git scoping, and the two ratification confirmations. In the excluded session, three more: the instruction to fix the rename measure and re-record the shape case, the ratification of TEST-0009 with the instruction to commit, and the request for this record. Four ratification sittings in all: fifteen items, then two new and one re-ratified, then two new, then one new.

## Verdict

Cost: 0.74 active hours of AI session, 307 thousand output tokens, 74 tool calls and 7 human turns in the measured session, plus an unmeasured half hour and three turns in another for the rename fix, for a script, a graph of eighteen items, nine tests, a catalogue entry, a collection and seven recorded cases; one requirement came from the human after reading a result, one from the AI reading its own first record, and one measuring case from the AI reading another skill's record; two of ten defects reached a pushed commit before CI caught them. Return: every number in the seven cases came from a command anyone can rerun at the commit named, the skill's own build is measured by the same rule as the work it measures, and the one wrong number the collection has held was found by reading a record and corrected by a re-record beside it rather than an edit. What it does not do: count anything the transcript does not hold, or judge the work it measures. Judgement: the measuring tool has now been wrong twice about what it measures, both times caught by reading its own output against the git history, and both times the fix cost less than the reading; the case for the tool rests on the reader as much as on the script, and the collection should be read that way.
